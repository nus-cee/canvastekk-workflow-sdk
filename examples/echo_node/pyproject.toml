[project]
name = "echo-node"
version = "0.5.0"
description = "Echo node example for CanvasTEKK Workflow SDK"
requires-python = ">=3.12"
dependencies = [
    "canvastekk-workflow-sdk>=0.5.0",
]

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
